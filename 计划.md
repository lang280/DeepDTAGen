# 下一步计划

当前状态：

- 阶段 1 已完成：`data/` 下 6 个 CSV、`models/` 下 3 个 `.pth` 已齐全。
- 阶段 2 已完成：已修复当前已发现的硬阻塞，并通过 `py_compile`。
- 阶段 3 已完成：项目根目录 `.venv` 已创建，Python 版本为 3.8，核心依赖导入检查通过。
- 阶段 4 已完成：`data/processed/{kiba,davis,bindingdb}_{train,test}.pt` 已生成。
- 阶段 5 已完成：demo、测试、生成、生成评估链路已在 CPU 上跑通。

## 下一步应做

### 1. 做训练链路短跑验证

目标：

- 确认训练脚本可以正常进入 epoch 循环。
- 确认 loss 能正常计算，且 checkpoint 能正常保存到 `models/`。

建议动作：

- 给 [training.py](/home/jianlang/DeepDTAGen/training.py) 增加可配置参数：
  - `--dataset`
  - `--device`
  - `--epochs`
  - `--eval-every`
  - 可选：`--batch-size`
- 保持默认行为不变，但允许用很小的 epoch 做 smoke test。

建议验证命令：

```bash
.venv/bin/python training.py 2
```

更推荐先改成参数化后再跑，例如：

```bash
.venv/bin/python training.py --dataset bindingdb --device cpu --epochs 1 --eval-every 1
```

完成标准：

- 能进入训练循环。
- 能完成至少 1 次测试评估。
- `models/` 下能看到新保存的权重文件。

### 2. 核对训练输出与现有权重命名策略

目标：

- 避免训练新权重覆盖作者原始预训练权重。

建议动作：

- 决定是否保留当前命名：
  - `models/deepdtagen_model_{dataset}.pth`
- 或改成新的训练命名：
  - `models/deepdtagen_model_{dataset}_finetune.pth`
  - `models/deepdtagen_model_{dataset}_smoke.pth`

建议：

- 如果接下来会反复做训练实验，最好不要直接覆盖作者原始权重。

### 3. 清理 demo 运行副产物策略

目标：

- 避免 demo 每次运行都在 `data/` 和 `data/processed/` 留下带时间戳的临时文件。

需要处理的副产物包括：

- `data/processed/2026*.pt`
- `data/*_tokenizer.pkl` 中由 demo 临时生成的非常规文件

建议动作：

- 决定是否修改 demo 代码，把临时文件写到：
  - 单独的临时目录
  - 或固定可清理的 demo 缓存目录

### 4. 形成最终提交边界

目标：

- 区分“应提交的代码修复”和“只本地保留的复现资产”。

建议提交的内容：

- 代码修复：
  - [utils.py](/home/jianlang/DeepDTAGen/utils.py)
  - [training.py](/home/jianlang/DeepDTAGen/training.py)
  - [model.py](/home/jianlang/DeepDTAGen/model.py)
  - [generate.py](/home/jianlang/DeepDTAGen/generate.py)
  - [generation_eveluation.py](/home/jianlang/DeepDTAGen/generation_eveluation.py)
  - [DEMO/required_files_for_demo/model_aff.py](/home/jianlang/DeepDTAGen/DEMO/required_files_for_demo/model_aff.py)
  - [DEMO/required_files_for_demo/model_gen.py](/home/jianlang/DeepDTAGen/DEMO/required_files_for_demo/model_gen.py)
- 文档：
  - [复现.md](/home/jianlang/DeepDTAGen/复现.md)
  - [计划.md](/home/jianlang/DeepDTAGen/计划.md)
  - [.gitignore](/home/jianlang/DeepDTAGen/.gitignore)

建议仅本地保留、不纳入追踪的内容：

- `.venv/`
- `data/*.csv`
- `data/*.pkl`
- `data/processed/*.pt`
- `models/`
- `models.rar`
- `*_generated.csv`

## 推荐执行顺序

1. 先把 `training.py` 参数化。
2. 做 1 个 epoch 的 CPU 训练 smoke test。
3. 确认 checkpoint 保存策略不覆盖原权重。
4. 再决定是否清理 demo 的临时产物逻辑。
